#ifndef COMPONENT_H
#define COMPONENT_H

#include <iostream>

// A type alias for a component (used like an index).
using ComponentType = std::uint32_t;

// Defines the size of the component arrays.
const ComponentType MAX_COMPONENTS = 32;

#endif // !COMPONENT_H