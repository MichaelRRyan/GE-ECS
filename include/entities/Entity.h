#ifndef ENTITY_H
#define ENTITY_H

#include <iostream>

// A type alias for an entity (used like an index).
using Entity = std::uint32_t;

// Defines the size of the entities arrays.
const Entity MAX_ENTITIES = 5000;

#endif // !ENTITY_H